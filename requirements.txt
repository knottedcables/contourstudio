# Runtime dependencies (what ships in the Docker image).
# Dev/test tooling lives in requirements-dev.txt so it never reaches production.

# Backend
fastapi
uvicorn[standard]

# DEM / geometry pipeline
numpy
scipy
shapely
contourpy
Pillow          # decode terrarium PNG tiles
requests        # fetch tiles / Overpass

# Exports
cairosvg        # PNG rasterization of SVG
